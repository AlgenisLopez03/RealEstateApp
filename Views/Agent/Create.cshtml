@* @using RealEstateApp.Core.Application.ViewModels.Property;
@model SavePropertyViewModel;

@{
    ViewData["Title"] = "Create";
}

<form asp-action="Create">
    <div class="form-group mb-3">
        <label asp-for="Description" class="control-label"></label>
        <input asp-for="Description" class="form-control" />
    </div>
    <div class="form-group mb-3">
        <label asp-for="Price" class="control-label"></label>
        <input asp-for="Price" class="form-control" />
    </div>
    <div class="form-group mb-3">
        <label asp-for="FieldSize" class="control-label"></label>
        <input asp-for="FieldSize" class="form-control" />
    </div>
    <div class="form-group mb-3">
        <label asp-for="Rooms" class="control-label"></label>
        <input asp-for="Rooms" class="form-control" />
    </div>
    <div class="form-group mb-3">
        <label asp-for="Bathrooms" class="control-label"></label>
        <input asp-for="Bathrooms" class="form-control" />
    </div>
    <div class="form-group mb-3">
        <label asp-for="PropertyTypeId" class="control-label"></label>
        <select asp-for="PropertyTypeId" class="form-control">
            <option value="">Select Type</option>
            @foreach (var type in Model.Types)
            {
                <option value="@type.Id">@type.Name</option>
            }
        </select>
    </div>
    <div class="form-group mb-3">
        <label asp-for="SellTypeId" class="control-label"></label>
        <select asp-for="SellTypeId" class="form-control">
            <option value="">Select Type</option>
            @foreach (var type in Model.Sales)
            {
                <option value="@type.Id">@type.Name</option>
            }
        </select>
    </div>
    <div class="form-group mb-3">
        <label asp-for="ImprovementId" class="control-label"></label>
        <select asp-for="ImprovementId" class="form-control">
            <option value="">Select Type</option>
            @foreach (var type in Model.Improvements)
            {
                <option value="@type.Id">@type.Name</option>
            }
        </select>
        <span asp-validation-for="ImprovementId" class="text-danger"></span>
    </div>
    <div class="btn-group mb-3 col-12">
        <a asp-action="Index" asp-controller="Home" class="btn btn-outline-primary w-full">Back</a>
        <input type="submit" class="btn w-25 btn-outline-success w-100" value="Create" />
    </div>
</form> *@